
users.get('/:id/edit', function(req,res) {
    users.findById(req.params.id)
    .exec(function(err, users) {
        if (err) { console.log(err); }

        res.render('users/edit', {
            users: users
        });
    });
});

